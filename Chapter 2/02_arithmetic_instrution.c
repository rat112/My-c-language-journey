#include<stdio.h>
#include<math.h>

int main(){
    // int a = 4;
    // int b = 8;
    // printf("The value of a + b is : %d \d",a + b);
    // printf("The value of a - b is : %d \d",a - b);
    // printf("The value of a * b is : %d \d",a * b);
    // printf("The value of a / b is : %d \d",a / b);

    // int z;
    // z = a * b; // Legal
    // // a * b = z; // Illegal
    // printf("The value of z is : %d \d",z);  int b = 8;

    // printf("5 divided by 2 leaves a remainder of %d \n", 5%2);
    // printf("5 divided by 2 leaves a remainder of %d \n", -5%2);
    // printf("5 divided by 2 leaves a remainder of %d \n", 5%-2);
    
    // //No operator is assumed to be present
    // // printf("the value of 4 * 5 is %d\n",(4)(5)); this is worng
    // printf("the value of 4 * 5 is %d\n",(4)*(5));

    // //There is on operater to perfrom exponentiation in c
    // // printf("the value of 4 * 5 is %d\n",4^5); //will not produce 4 to the power 5 
    // printf("the value of 2 to the power 5 is  %f\n",pow(2 , 5));

    // printf("The value of 4 + 6 is %d\n",4 + 6);
    // printf("The value of 4 + 5.6 is %f\n",4 +5.6);
    // printf("The value of 4.1 + 5.6 is %f\n",4.1 +5.6);
    // printf("The value of 5 / 2 is %d \n",5 / 2);
    // printf("The value of 5 / 2 is %f \n",5.0 / 2);
    
    // this is not recommend 
    // int a = 5.5;
    // printf("The value of a is %d\n", a);
    // float b = 5;
    // printf("The value of b is %f\n", b);

    // printf("The value of k is %f \n", 3.0 / 9);



    return 0;
}